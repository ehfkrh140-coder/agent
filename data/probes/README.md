# Probe outputs

This directory may hold local read-only public probe JSON reports such as `usdt_krw_public_probe_fx_hardened.json`. Probe outputs are planning artifacts only: they are not OpportunityPackets, adapters, readiness decisions, Council handoffs, orders, balances, transfers, or auto-trading instructions.
